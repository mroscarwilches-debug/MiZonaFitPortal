{
  "name": "warrior-code-portal",
  "version": "1.0.0",
  "private": true,
  "description": "El Código del Guerrero — fitness portal (static site)",
  "type": "module",
  "scripts": {
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "1.53.0",
    "vitest": "^3.2.0"
  }
}
